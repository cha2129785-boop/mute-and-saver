# Changelog

## v1.4.38 (release)
- 서식 리본 'TXT 내보내기' 옆에 'TXT 불러오기' 추가 — .txt 파일을 새 메모로 불러옴(제목=파일명, 본문=내용). utf-8/utf-8-sig/cp949 자동 인코딩 감지. 메모 개수 한도는 기존 추가 로직이 처리. 버튼/메시지 5개국어.

## v1.4.37 (release)
- 설정 탭 프리셋 카드에서 이름이 너무 길어 카드 높이를 넘겨 잘리던 것 → 표시용 이름 말줄임(…, 최대 15자) 처리. 선택·저장은 전체 이름 유지.

## v1.4.36 (release)
- 설정 탭 프리셋 카드를 미디어 썸네일과 동일한 정사각형(104×104)으로 변경(기존 150×52 가로로만 넓던 것). 이름 중앙 정렬·14pt bold·2줄 줄바꿈.
- 첫 실행 창 높이를 설정 탭 내용 높이에 맞춰 스크롤바 없이 딱 떨어지게(center_window 측정 직전 캔버스 높이=내용 높이). 창을 줄이면 세로 스크롤바 자동 표시/숨김.

## v1.4.34 (release)
- 창 리사이즈 크래시 근본 수정 — 이벤트 뷰어에서 예외 코드 `0xc000041d`(STATUS_FATAL_USER_CALLBACK_EXCEPTION, 모듈 python314.dll) 확인. Windows 리사이즈 모달 루프 중 `<Configure>` 콜백에서 파이썬 예외가 커널 콜백 경계로 탈출하면 OS가 프로세스를 강제 종료(faulthandler·report_callback_exception도 포착 불가). 설정탭·미디어탭 리사이즈 `<Configure>` 핸들러 3곳을 광범위 try/except로 감싸 예외 격리 + 실제 예외를 `[Diag] …예외 격리` 로그로 남김.

## v1.4.33 (release)
- 창 리사이즈 네이티브 크래시 조사: fault.log가 생성되나 비어있음(faulthandler 미포착) + [TkCallback] 없음 → 스택 오버플로(재진입 폭주) 유력. 설정탭·미디어탭 `<Configure>` 핸들러에 재진입 가드 추가(재귀 시 차단) + 재진입 발생 시 1회 진단 로그(`[Diag] …재진입 차단`). 재현 시 이 로그로 재귀 여부 확진.

## v1.4.32 (release)
- 잠재 버그 수정: `init_runtime()`(faulthandler·sys.excepthook·번들 폰트)과 `ensure_single_instance()`(중복 실행 방지)가 `__main__` 가드 안에만 있어, run.pyw/빌드 실행 경로(import 후 main() 호출)에서 **전혀 실행되지 않던** 문제 → `main()` 최상단으로 이동. 이제 네이티브 크래시가 `fault.log`에 C-스택으로 기록됨(리사이즈 크래시 원인 추적용). 로그 폴더 자동 생성 보강.

## v1.4.31 (release)
- 설정 탭 프리셋 카드 고정 크기가 실제로 적용되지 않던 문제 수정 — 카드 내부가 pack 배치라 `grid_propagate(False)`가 무효였음 → `pack_propagate(False)`로 교정. 이제 모든 카드 150×52 균일.

## v1.4.30 (release)
- 미디어·프리셋 등 한도 도달 시 'Pro 구매/업그레이드' 유도 팝업 제거 → '추후 한도 추가 업데이트 예정' 안내로 통일(3곳). 메모 수 한도 안내에도 동일 취지 문구 추가. (Pro 업그레이드 다이얼로그는 호출 안 함)

## v1.4.29 (release)
- 서식 리본 표 섹션 뒤에 '레이어(맨 앞으로/맨 뒤로)' + 'TXT 내보내기' 추가. 레이어=선택 메모 z-order(기존 우클릭 기능 리본화), TXT 내보내기=선택 메모를 .txt로 저장(제목+본문, 표는 탭 구분 행. 파일명=메모 제목). 버튼/메시지 5개국어. (전체 메모 저장은 편집 메뉴의 세트 내보내기로 별도 제공)

## v1.4.28 (release)
- 설정 탭 프리셋 카드를 미디어 썸네일처럼 고정 크기(150×52)로 통일 — 내용 길이에 따라 카드 폭이 들쭉날쭉하던 것 개선. 긴 이름은 2줄 줄바꿈.

## v1.4.27 (release)
- 프로그램 시작 창이 작게(높이 하한 580) 열려 답답하던 문제 개선 — 기본 창 크기 상향(420×760), 화면 밖으로 넘치지 않게 화면 상한 클램핑. 저장된 창 크기도 최소 420×700으로 복원.

## v1.4.26 (release)
- 서식 리본 대상별 재배치: 선택표시 │ 테마색 │ 글꼴 │ 제목(크기·색) │ 본문(크기·색·줄글자·줄글자지우기) │ 정렬(제목·본문·화살표) │ 이모지 │ 표(행·열·삽입삭제·셀색·셀색지우기). 제목/본문 컨트롤을 대상별로 묶고, 이모지 앞 라벨 표기. 기능·핸들러 불변, 배치만 정리. (줄글자=표 선택 시 셀글자로 동적 전환 유지)

## v1.4.25 (release)
- 메모 카드 리사이즈(축소) 시 그림자·선택 테두리 잔상이 남던 버그 수정 — 리사이즈 중·완료 시 그림자·테두리 사각형 좌표를 카드 새 크기에 동기화. (이동은 기존에도 정상, 리사이즈만 누락됐던 것)

## v1.4.24 (release)
- 트레이 '화면보호기 미리보기' 항목 제거 — 껍데기(빈 창)만 뜨던 미작동 기능. '잠금'으로 실제 실행 가능하므로 중복 제거.
- 리본 글자색 드롭다운: 어두운 메뉴 배경에서 '검정' 항목이 안 보이던 문제 수정 — 어두운 색은 밝게 표기.
- 메모 편집: 화면보호기 모드가 memo/split이 아니어도 버튼으로 실행 가능(단축키 경로와 동일). 기존 모드 제한 안내 제거.
- 메모보드 탭: 'python.exe 잔상' 경고 문구 삭제.
- 리본 라벨: 배경색/표 테마 동적 전환 → '테마색'으로 공통 표기.

## v1.4.23 (release)
- 정보 탭에 '업데이트 확인'(옵트인 수동) 버튼 추가 — 클릭 시에만 GitHub 최신 릴리스 버전을 1회 조회(백그라운드), 새 버전이면 다운로드 페이지 열기 안내. 백그라운드 통신·사용자 데이터 전송 없음. 버튼/메시지 5개국어.

## v1.4.22 (release)
- 본문 편집 중 리본에서 스타일(본문 크기·색·정렬·제목 크기 등) 변경 시 입력한 내용이 사라지던 버그 수정 — 카드 재생성 전 편집 중 Text 내용을 memo에 선반영. (본문 크기뿐 아니라 편집 중 모든 리본 조작에 적용)
- 제작자 표시 영문 병기: `슈글` → `슈글 (Shugle)`.

## v1.4.21 (release)
- 달력 제목 다국어화: 기존 `{y}년 {m}월` 하드코딩 → `cal_title` i18n 키(5개국어). 언어 변경 시 제목도 반영(영 `2026-03`, 中/日 `年月`, 러 `03.2026`). 요일 표기는 기존에도 정상.

## v1.4.20 (release)
- 사용설명서 5개국어 완성: 한글·영문·中文·日本語·Русский. 언어에 따라 자동 선택(미지원 언어는 영문). 미디어 카드 라벨도 언어별.

## v1.4.19 (release)
- 사용설명서 다국어화: 한글(`manual.png`) + 영문(`manual_en.png`) 제공. 언어에 따라 자동 선택(ko=한글, 그 외=영문). 언어 변경 시 다음 실행에 반영.
- 사용설명서 이미지 생성기 추가(`tools/make_manual.py`, 개발용·배포 미포함): HTML→Chromium 렌더로 전 언어·버전 배지 자동 생성.

## v1.4.18 (release)
- EULA 1.2 개정: 4조(책임 제한)에 "꼭 필요한 내용은 반드시 별도로 백업·저장" 안내 추가. (기존 동의자 재동의 1회)
- 도네이션 안내 문구의 괄호 보충설명 삭제.
- 정보 탭에 '설정 백업 / 복원' 기능 추가 — 설정·메모·즐겨찾기·테마·프리셋·사용자 이미지를 zip 하나로 저장/복원(복원은 재시작 후 적용).

## v1.4.17 (release)
- 사용설명서 이미지 갱신: 상단 앱 아이콘을 슈글 아이콘으로 교체, 버전 표기 v1.4.17로 수정.

## v1.4.16 (release)
- 사용설명서(기본 제공)가 미디어 목록에서 누락되던 문제 수정 — 매 실행 시 없으면 자동 복구, 설치 위치가 바뀌면 경로 자동 교정.
- 기본 제공 사용설명서는 미디어 갯수 제한(무료 한도)에 포함하지 않음.

## v1.4.15 (release)
- 이미지 메모가 화면보호기에서 안 보이던 버그 수정 — 기본(투명도 0) 렌더 경로(_place_card)에 이미지 폴라로이드 처리 추가. (기존엔 PIL 합성 경로에만 존재)
- 작업표시줄 아이콘: 고유 AppUserModelID 지정으로 창 아이콘(app.ico) 표시줄·핀 반영.

## v1.4.14 (release)
- 창 타이틀바 아이콘·시스템 트레이 아이콘을 app.ico(앱 아이콘)로 적용. 기존 존재하지 않는 icon.ico를 참조하던 문제 수정.

## v1.4.13 (release)
- 설정 탭 세로 스크롤 적용 — 폰트 확대로 하단(잠금화면 메시지)이 잘리던 문제 수정.
- 대기 시간: 슬라이더·구분선 제거, 직접 입력 방식만 유지. '대기 시간' 라벨 확대(13), 라벨 굵게 해제(보통).

## v1.4.12 (release)
- 전 탭 서브메뉴·라벨 글자 크기 확대: 중앙 ttk 스타일(LabelFrame 제목·체크·라디오·버튼·콤보 등) 10→13, 하드코딩 라벨(값·힌트·썸네일) 9/10/11→13.

## v1.4.11 (release)
- 단축키 탭: 퀵 메모 예시·잔존 문자열의 'Ctrl+M' 표기를 실제 기본값(Ctrl+Alt+;)에 맞게 교정. (기본값 상수는 기존에도 정상)
- 화면보호기·퀵메모 카드 레이아웃 완전 대칭 통일(상태 라벨 양쪽 동일).
- 단축키 탭 글자 크기 확대(본문 13 / 현재값 18)로 식별력 향상.

## v1.4.10 (release)
- 정보 탭 제작자 표시를 '슈글'로 교체, 개인 이메일 비노출. 라이선스 탭 히든 언락(제작자 라벨 클릭)은 그대로 동작.

## v1.4.09 (release)
- EULA 1.1 개정: 1조 '무료·자유 사용·배포' 문구 삭제, 6조 구체 법령(저작권법·정보통신망법) 삭제, 7조(준거법) 삭제. EULA_VERSION 1.1 → 기존 사용자 재동의 1회.

## v1.4.08 (release)
- 겹친 메모의 제목바/카드 클릭 시 항상 맨 앞으로 이동(이미 선택된 경우도 포함).
- 선택 초록 테두리가 겹친 다른 메모에 가려 끊기던 현상 수정 — 테두리를 카드 위젯 자체에 표시.

## v1.4.07 (release)
- 신규 메모 기본값 변경: 테마 시트린, 제목 32pt, 본문 24pt, 글자색 검정, 정렬 왼쪽. (기존 메모 무영향)

## v1.4.06 (release)
- 우클릭 정렬 메뉴: 표 메모에서 '본문' 대신 '셀' 정렬 항목 추가(기존엔 제목만 표시됨). 셀 우클릭 메뉴에도 정렬 추가. 선택 셀 대상.

## v1.4.05 (release)
- 첫 실행 시 사용권 계약(EULA) 동의창 추가 — 끝까지 읽어야 '동의' 활성, 비동의 시 종료. 동의 후 재표시 없음(계약 버전 갱신 시 재동의).
- `EULA.md`(한/영) 문서 추가.

## v1.4.04 (release)
- 행/열 메뉴 아이콘 표기 교정(행=↔, 열=↕). 동작 불변.
- 제목 편집 저장 순서 버그 수정(제목 이모지·변경 유실 방지).
- 이모지 팔레트·테마 라벨을 Windows 흑백 폴백 호환 이모지로 정리.

## v1.4.02 (release)
- 이모지: Windows 흑백 폴백 폰트(Segoe UI Symbol) 호환 이모지로 팔레트 정리(신형/변형 이모지 제외) → 저장·표시 안정화. 테마 라벨 이모지도 구형 호환으로 교체.

## v1.4.01 (release)
- 테마색을 젬스톤 6종으로 정리: 자수정·투어마린·사파이어·시트린·루비·진주 (나머지 기본테마 삭제).

## v1.4.00
- 이모지: 내장 팔레트(😀, 64종, 흑백) + OS 이모지창(Win+.) 병행 — 본문·셀·제목 삽입.
- 이미지 메모(폴라로이드): 삽입·캡션·리사이즈·교체, 화면보호기 렌더.
- 표: 셀 배경색·글자색·정렬, 위치 기준 행/열 삽입·삭제, 테마색 바깥 테두리, 격자 WYSIWYG.
- 정렬: 제목/본문/셀 (리본 제목|본문 타깃 토글 + 우클릭 정렬▸제목/본문).
- 사용자 커스텀 양식 저장/삭제·즐겨찾기 바로가기, Delete/Insert 단축키.
- 안정성: 메모 사라짐 버그 근본 수정, 그림자 잔상 제거, 배경/제목바 클릭 해제.
- UI: 상단 버튼 규격·색 통일(주황/에메랄드/그레이), 리본 테마색 프리셋(꽃 이름 11색), 달력 화살표 확대.

## v1.3.x — 표/달력/서식 리본 기반
- 메모 표(행/열·크기), 달력(월 이동·내용 보존), z-order, 서식 리본(글꼴·크기·색).
- pycaw/COM GC 크래시 근본 수정, HiDPI, 창 크기·위치 기억, 트레이 상태.

*(상세 이력: 프로젝트 내 research_v1.md)*
